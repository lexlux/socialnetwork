extends layouts/main-layout.pug

block content    
    .chatPageContainer
        .chatTitleBar
            label(for="userSearchTextbox") To:
            #selectedUsers
                input#userSearchTextbox(type="text", placeholder="Введите имя человека")
        .resultsContainer
        button#createChatButton(disabled) Создать чат